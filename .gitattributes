# Images are stored as normal Git blobs (no Git LFS).
# Mark them binary so Git never tries to diff/merge or alter line endings.
*.jpg  binary
*.jpeg binary
*.png  binary
*.tif  binary
*.tiff binary
*.webp binary
